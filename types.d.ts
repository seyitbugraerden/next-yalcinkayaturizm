interface sectionTitleTypes {
  title?: string | "Test";
  mainTitle?: string;
  white?: boolean;
  description?: string;
}

interface homeServiceTypes {
  text: string;
}

interface offerCardTypes {
  features: any;
}
